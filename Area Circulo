#Diseñe una función llamada area_circulo que reciba como parámetro el valor de un radio y devuelva el área de un círculo con dicho radio.
import math
def area_circulo (radio):
    area = math.pi*pow(radio,2)  
    return area
#Programa Principal
radio = float(input("Ingrese el radio de un círculo en cm: "))
print("El área del círculo de radio {0:.2f}".format(radio), "cm es: {0:.2f}".format(area_circulo(radio))," cm2")
